# 交接状态 · 2026-08-12(v81,V 拍板日下午)

> 给下一个接手的 Claude:先读 `启动PROMPT.md` 和 `CLAUDE.md`(铁律),再读本文件。
> 本文件取代 `交接状态-2026-08-11.md`,冲突以本文件为准。

## 对 V 的沟通规矩(不变)

- 一律中文;先提案后动手,一次一件(V 明确点名的除外);Confluence 只在 V 喊时同步;
- HTML 预览走 rawgithack:`https://raw.githack.com/vvv1088/PI/claude/new-hypothesis-restructure-rvbpn5/PI/demo.html`;
- 容器时钟不准,真实日期以对话为准;分支只准 `claude/new-hypothesis-restructure-rvbpn5`。

## 2026-08-11 一天内发生的事(v70 → v80,全部已推送)

- **v71**:系统 2 全部 18 页 1:1 搬进 MIS(16 新视图 + mock 扩展 `mis-meta-admin-mock.js`)。
- **v72**:页面收编(Asset Status→Overview、Rotation Log→Rotation Logs tab、Action Logs→Activity Log)+ 导航 6 组默认收起 + Creative 图片修复 + 预算月份 15 号提前生成(V 定)。
- **v73–v76 命名工程**(V 逐项确认过对照表):`mis-naming.js` 生成/解析/发号引擎;Supabase 落库(brand_aliases 15 行、dict short_code + Ad Setting tab、hypotheses.ad_setting/ref_batch、creatives.ref_code 唯一);市场统一 USC/MY/SG(存量 50 条 KH 已迁);表单接主数据(INZ9 出 MY/SG);建素材自动正式发号;「⚙ 生成」一键出全名。
- **v77**:两套 Users 统一(`meta_user_map` 表)+ Activity Log 合并单表 + 换版 V 后缀名 + retired 品牌「已整合」标记(方案 A:历史留旧名)。
- **v78**:实体连接 L1–L6(品牌全景 4 面板、全局品牌/假设可点、Spending/Our Ads 挂 🧪 假设、闭环页 MAIN pixel 健康徽章、轮转影响面)+ **三层归因**(严格 ref → 登记全名 → 品牌;未归因清单=过渡期工作清单)。存量 23 条旧式 ads_code 靠第 2 层登记制覆盖,零回填。
- **v79**:字典显示短码;Ad Gallery 改名 Our Ads;**live 只读总闸门 `allowMetaWrite`**(14 个写入口统一拦);引擎 9 项回归检查进冒烟。
- **v80**:三份设计文档(docs/04 拆文件、05 权限映射、06 baselines)+ `mis-baselines.js` 休眠实现(`useLiveBaselines` 默认关)。
- **线 B 复活**:交接 zip 里带系统 2 完整仓库(scratchpad;含 git)。已产出 **patch 0002(S1 审计脱敏+存量清洗 SQL)、0003(B1/R1/R3 轮转加固)**,tsc+build 双过,在 `patches/`。注意:zip 里的轮转代码比 06 手册新(R2 已修),患处以 patch 说明为准。

## 2026-08-12 白天:V 拍板全部决策 → v81 三段施工完毕

- **决策**:拆文件=方案 A;权限映射=17 key 照搬 + **PE=PO** + 审计对人要;baselines=窗口 OK + BO 已有 reg_count + 不并行直接切;**ads code 终版 7 段**(经多轮讨论:TR 定位与语言段砍、维度用 short form FM/HK/VS/OF/GT、编号=3 位流水按品牌×内容全局递增+优化 V 后缀、市场段保留、方案一登记制)。
- **v81a 命名契约 v2**:mis-naming.js 重写(见 `docs/07-命名契约v2.md`);全维度词条短码草稿已落 Supabase(**V 要过目**,字典页可改);officialRefs/ref_batch 退役;引擎回归 11 项。
- **v81b 权限映射**:role_meta_permissions 表+默认值已建;Roles 抽屉 Meta 区;导航/写按钮 per-key gating;metaApi 带 X-MIS-User;**patch 0004**(审计对人,依赖 0001 先应用)tsc+build 过。
- **v81c 拆文件 A**:src/ 五块 + template + tools/build.js;产物逐字节对齐;**index.html 自此是构建产物——改动落 src/,`node tools/build.js` 后再冒烟**。
- baselines 收口(v80.1):REG 月化进 compute;文档删并行期。

## 等 V 的(新)

1. 字典短码草稿过目(docs/07 有全表;不对的直接在 Dictionary 页改,引擎自动跟);
2. cPanel 发版包 **v81 版 10 文件**(`PI/release/`)等上传;
3. patch 0001–**0005** 转交 Jayden(按序 git am;**0005**=P2 双 token(读/写两把钥匙一次发好;开写=我们自己换 token 发版,Jayden 零二次动作)——取代 F 节"分批开放")。

## 阻塞(不变)

- Jayden 未回对接清单(docs/02):live 切换、BO 通道 D 节、E 节打勾、G 节监测范围、P0 备份确认。
- V 明确:第五类(提醒系统/Gemini 实验等另一条线)本工作区**不处理**。

## 环境备忘

- 冒烟:`PI/tools/smoke-test.js`(playwright 本环境可 npm 安装,NODE_PATH 指 scratchpad/node_modules;35 视图+交互+11 项引擎检查)。
- **改前端后(v81 起)**:改 `src/`(0*.js / index.template.html)→ `node tools/build.js` → node --check → gen_demo.py 重生成 → 跑冒烟 → 版本快照到 versions/。mis-*.js 照旧直接改。
- Supabase MIS 项目 `bfukphakofrjalsqteda`;已建表:brand_aliases、meta_user_map、**role_meta_permissions**;dict_entries.short_code 全维度已填草稿。
- 发版=10 文件(index.html + 9 个 mis-*.js);`MIS_META` 开关:mode/token/allowRemarkEdit/allowMetaWrite/useLiveBaselines。
