# 系统 2 → MIS 搬家映射表

> 2026-08-11 初版。三个用途：① MIS 侧建页面的施工图；② 对照 Jayden E 节打勾结果核对接口可用性；③ P2 工作量估算底稿。
> 阶段口径：**P1** = 接入改造部署后 2–3 周内（只读）；**P2** = 操作搬家；**Vny 阶段** = 深度分析模块（PBI 内容并入 MIS），本轮不做。

## 一、PERFORMANCE 组（P1 主战场）

| 系统 2 页面 | 功能 | MIS 目标位置 | 所调接口（06 手册编号） | 读/写 | 阶段 |
|---|---|---|---|---|---|
| `/analytics/spending` | 按 line 花费明细 + 备注 + 导出 | PERFORMANCE → Spending 明细 | #33 GET spending、#34 GET export、#35 PUT remark（默认只显示，编辑待 Jayden 开 `MIS_ALLOW_REMARK_PUT`） | 读（+1 写例外） | **P1** |
| —（新建，系统 2 无此页） | 测试闭环归因：花费×FD/D7 落到 hypothesis/creative（ref code join） | PERFORMANCE → 闭环报表 | #33 GET spending（花费侧）+ BO 通道（D 节待 Jayden 拍板）+ Supabase hypotheses/creatives | 读 | **P1** |
| `/analytics/accounts` | 单账户花费/转化趋势 | PERFORMANCE（后续） | #28 | 读 | Vny 阶段 |
| `/analytics/ads` + `/ads/[id]` | 广告区间指标/日明细 | PERFORMANCE（后续） | #29、#30 | 读 | Vny 阶段 |
| `/analytics/brands` | 品牌对比矩阵 | PERFORMANCE（后续） | #31 | 读 | Vny 阶段 |
| `/analytics/lifecycle` | 资产存活周期统计 | PERFORMANCE（后续） | #32 | 读 | Vny 阶段 |

## 二、ASSETS 组（P1 精选三视图 → P2 全量+操作）

**P1 精选（只读，有独立日常价值）：**

| 新视图 | 聚合内容 | 所调接口 | 阶段 |
|---|---|---|---|
| Health 体检总览 | 每日健康检查结果 + 品牌×角色网格 | #24 GET health、#8 GET pixels、#2 GET brands | **P1** |
| 轮转历史 | 轮转/状态变更审计流水 | #21 GET rotation/logs | **P1** |
| 资产状态总览 | 四类资产的数量/状态汇总卡（ACTIVE/BANNED/DISABLED） | #2、#5、#8、#10 各 GET 列表 | **P1** |

**P2 全量（页面 + 操作）：**

| 系统 2 页面 | 功能 | MIS 目标位置 | 所调接口 | 读/写 | 阶段 |
|---|---|---|---|---|---|
| `/brands` + `[id]` | 品牌 CRUD | ASSETS → Brands | #2、#3 | 读+写 | P2 |
| `[id]` 内 CAPI 开关 | 品牌×事件开关 | ASSETS → CAPI 事件开关 | #4 GET/PUT | 读+写 | P2 |
| `/business-managers` + `[id]` | BM CRUD + 封禁 | ASSETS → BM | #5、#6 | 读+写 | P2 |
| `[id]` 内 FB 个人号 | 1+2 机制管理 | ASSETS → BM 详情内嵌 | #7 | 读+写 | P2 |
| `/pixels` + `[id]` | Pixel CRUD | ASSETS → Pixels | #8、#9 | 读+写 | P2 |
| `/ad-accounts` + `[id]` | 账户 CRUD + 品牌关联 | ASSETS → Ad Accounts | #10、#11、#12 | 读+写 | P2 |
| `/developer-apps` + `[id]` | 应用 CRUD | ASSETS → Apps | #13、#14 | 读+写 | P2 |
| `/tokens` + `[id]` | Token 管理 | ASSETS → Tokens（**只显示脱敏元数据**；明文值不进 MIS，操作场景另议） | #15、#16 ⚠️ 现返回明文，需 Jayden 脱敏或 P2 再议 | 读+写 | P2 |
| `/pixel-shares` | 共享登记 | ASSETS → Pixel Shares | #17、#18 | 读+写 | P2 |
| `/rotation` | 轮转执行 | ASSETS → 轮转（执行） | #19、#20 POST | **写（最高危）** | P2 后段 |
| `/sop`、`/[instanceId]`、`/templates` | SOP 任务与模板 | ASSETS → SOP | #22、#23 | 读+写 | P2 |
| `/health` | 健康页 | （P1 已建总览，补明细钻取） | #24 | 读 | P2 |

## 三、ADMINISTRATION 组（P2）

| 系统 2 页面 | 功能 | MIS 目标位置 | 所调接口 | 读/写 | 阶段 | 前置 |
|---|---|---|---|---|---|---|
| `/users` + `[id]` | 用户管理（9 账号） | ADMIN → Users（**两边账号合并**） | #25、#26 | 读+写 | P2 | 权限映射设计（待办 7） |
| `/action-logs` | 操作审计 | ADMIN → Activity Log（合并展示两边审计） | #27 ⚠️ 明文渲染 token/secret，**脱敏前不接** | 读 | P2 | Jayden 侧脱敏 |
| 首页仪表盘 | 概览统计 | 不搬（MIS 各模块自带入口；如需概览卡后续加） | — | — | 不搬 |

## 四、口径与统计

- P1 建设量：PERFORMANCE ×2 页 + ASSETS 精选 ×3 视图 = **5 个新视图**，全部只读（remark 编辑除外）
- P2 建设量：资产 CRUD ×9 页 + 轮转执行 + SOP + 用户/审计 = **约 13 个视图 + 写操作**
- 接口缺口（对照 E 节）：BO 通道（D 节）、tokens 脱敏（#15/16）、action-logs 脱敏（#27）、page/profile 状态（G 节）
- 轮转执行（#19/20）是全表**唯一高危写操作**，放 P2 最后一批，上线前需与 Jayden 约定演练
