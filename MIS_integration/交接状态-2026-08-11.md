# 交接状态 · 2026-08-11(v70 合入完成后)

> 给下一个接手的 Claude:先读 `MIS_integration/启动PROMPT.md` 和 `CLAUDE.md`(铁律),再读本文件。本文件是最新事实,与启动PROMPT 冲突时以本文件为准。

## 对 V 的沟通规矩(必须遵守)

- **一律用中文回复**。
- **先提案、后动手**:除了 V 明确点名的任务,任何改动先说方案,V 说"可以/做"才执行。一次只做一件事。
- **Confluence 文档包**:平时改动**不要**去同步 Confluence,只有 V 明确说"update 到 Confluence"时才做(V 的原话规矩)。
- 交付 HTML 预览用 rawgithack:`https://raw.githack.com/vvv1088/PI/claude/new-hypothesis-restructure-rvbpn5/PI/demo.html`;发给 V 的文件用 HTML(markdown 附件他那边渲染不了)。
- 容器时钟不准,**真实日期以对话里 V 的信息为准**(现在约 2026-08-11)。

## 刚完成(本 session)

- **v70 已合入并推送**(commit ebc4c03,分支 `claude/new-hypothesis-restructure-rvbpn5`,只准推这条分支):
  - PI/index.html = v69 + 纯增量三处(导航组 / 5 个空挂载 section / 3 个 script 标签);v69 快照在 `PI/versions/index_v69.html`。
  - 新代码全在 `PI/mis-meta-api.js`(mode:'mock',metaApi() 唯一数据入口)/ `mis-performance.js` / `mis-assets.js`;冒烟脚本 `PI/tools/smoke-test.js`。
  - 22/22 视图无头冒烟零报错。注意:工作区自带 playwright 冒烟**没跑成**(环境装不了 playwright),用 chromium 无头等效替代——已向 V 如实说明。
  - CHANGELOG 已写 v70 条目;demo.html 已重生成;preview 链接已发给 V。
  - 无头截图里的红条"marked is not defined"是无网环境加载不到 CDN 的假报错,线上没有。
- **发版方式**:v70 起 4 文件一起上传 cPanel(index.html + 3 个 mis-*.js),站点 `https://ci.boostmarketing.site`。

## 等 V 点单的四个可独立推进项(已报给 V,他点哪个做哪个)

1. 权限映射设计:系统 2 的 9 账号 → MIS 角色。
2. 拆文件 + 最简构建方案(P2 前必须)。
3. baselines 替换设计:New Hypothesis 基线改由 PERFORMANCE 供数,之后下线 `get_brand_baselines` RPC。
4. v70 四文件上 cPanel(mock 模式先给团队看)。

## 阻塞

- **Jayden 未回对接清单**(`docs/02-给Jayden的对接清单.md`):live 切换(mis-meta-api.js 改三行:mode:'live'+baseUrl+token)、BO 通道(FD/D7)三选一、`patches/0001` 部署,全卡在他。
- 系统 2 仓库(../Meta_Ads_Unified_Management_System)不在本环境,线 B 工作做不了。

## 其他在飞的事(非本工作区,但 V 随时会问)

- **提醒系统**(Supabase RPC `mis_reminders` + n8n adammkt):已修好 Split Messages bug($input.all()),瓶颈催办制 20/23/26/29 已上线,发送计划催办 25/27/30 已上线。**首次真实开火 8/20**;**8 月底 n8n 配额有爆额风险**(26/29/30 号催办压在上次爆额日期段)。n8n MCP 只连得到 adammkt,连不到 ohmeidaa。
- **Gemini→Claude 图片分析迁移**:视频(64% 素材)是硬阻塞,已提"30 广告缩略图对比实验"方案,**V 未批,别自己跑**。
- Confluence 文档 00-06 已同步到 8-11 状态(v70 未同步——等 V 喊)。00 页 V 曾改名"00 · Transparency Over Reporting",我的覆盖可能压过他的小改,历史版本可找回。
- V 自己的测试数据(created_by=vvv)已清干净;GG 是真同事,数据保留。

## 环境备忘

- 干活前 `cd /home/user/PI`(复合命令里别单独 cd,会破坏后续路径)。
- 改 index.html 后:提取内联 JS 跑 `node --check`;用 `PI/tools/gen_demo.py` 重生成 demo.html;chromium 无头(`/opt/pw-browsers/` 下)跑全视图冒烟。
- Supabase/n8n/Slack/Confluence 都走 MCP;Supabase 若权限失效让 V 重连。
